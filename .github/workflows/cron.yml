name: Hourly Cron
on:
  schedule:
    - cron: '0 * * * *'  # every hour
jobs:
  call-api:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger cron endpoint
        run: curl -X GET https://hmlml-jade.vercel.app/api/sync-hourly